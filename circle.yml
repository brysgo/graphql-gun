machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

test:
  override:
    - ./node_modules/.bin/jest --forceExit --env=jsdom
    
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn